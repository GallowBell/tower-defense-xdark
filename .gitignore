node_modules/
dist/
coverage/
.vite/

# tsc output for the config files — vite.config.ts / vitest.config.ts are the
# sources. Vite resolves vite.config.js BEFORE vite.config.ts, so a committed
# or stale emit silently shadows the real config.
vite.config.js
vite.config.d.ts
vitest.config.js
vitest.config.d.ts
*.tsbuildinfo
